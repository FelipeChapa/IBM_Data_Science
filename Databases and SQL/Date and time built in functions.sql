-- Drop the PETSALE table in case it exists
DROP TABLE PETSALE;
-- Create the PETSALE table 
CREATE TABLE PETSALE (
    ID INTEGER PRIMARY KEY NOT NULL,
    ANIMAL VARCHAR(20),
    QUANTITY INTEGER,
    SALEPRICE DECIMAL(6,2),
    SALEDATE DATE
  );
-- Insert saple data into PETSALE table
INSERT
  INTO PETSALE
  VALUES 
	(1,'Cat',9,450.09,'2018-05-29') ,
	(2,'Dog',3,666.66,'2018-06-01'),
	(3,'Dog',1,100.00,'2018-06-04'),
	(4,'Parrot',2,50.00,'2018-06-04'),
	(5,'Dog',1,75.75,'2018-06-10'),
	(6,'Hamster',6,60.60,'2018-06-11'),
	(7,'Cat',1,44.44,'2018-06-11'),
	(8,'Goldfish',24,48.48,'2018-06-14'),
	(9,'Dog',2,222.22,'2018-06-15');
	
	SELECT SUM(QUANTITY)
  FROM PETSALE;
	SELECT
   AVG(SALEPRICE/QUANTITY) AS AVG_$_PER_QUANTITY
  FROM PETSALE;
	SELECT  MAX(QUANTITY)
  FROM PETSALE;
	SELECT MAX(QUANTITY) AS QUANTITY_DOGS
  FROM PETSALE
  WHERE LOWER(ANIMAL) = 'dog'; 
	SELECT ANIMAL, ROUND(SALEPRICE)
  FROM PETSALE;
	SELECT ANIMAL, (CURRENT DATE - SALEDATE) AS TIME_PASSED
  FROM PETSALE
  ORDER BY ANIMAL;
	
	SELECT ANIMAL, ROUND(AVG(SALEPRICE)), COUNT(*) AS NUM_CATS
  FROM PETSALE
  GROUP BY ANIMAL;
	
	SELECT ANIMAL, LENGTH(ANIMAL)
  FROM PETSALE;

	 
